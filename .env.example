# Supabase Database URL (for cloud deployment)
# Get this from: Supabase Dashboard > Project Settings > Database > Connection String (URI)
# Format: postgresql://postgres.[project-ref]:[password]@[host].pooler.supabase.com:6543/postgres
SUPABASE_DATABASE_URL=postgresql://postgres.xxx:your-password@aws-0-region.pooler.supabase.com:6543/postgres

# ==================== API Keys ====================
LLAMA_CLOUD_API_KEY=your_llama_cloud_api_key
OPENAI_API_KEY=your_openai_api_key
DEEPSEEK_API_KEY=your_deepseek_api_key

# Supabase Configuration (for Storage)
# Project URL (from Project Settings > API)
SUPABASE_URL=https://your-project-ref.supabase.co

# Service Role Key (from Project Settings > API > service_role)
# ⚠️ This key has full access - keep it secret, only use on backend!
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Storage bucket names
SUPABASE_PAPERS_BUCKET=papers
SUPABASE_REPORTS_BUCKET=reports

# ==================== LangChain/LangGraph Configuration ====================
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your_langchain_api_key
LANGCHAIN_PROJECT=paper_helper

# ==================== Backend Configuration ====================
# CORS allowed origins (comma-separated, no spaces around commas)
# Add your production frontend URLs here
# Example: BACKEND_CORS_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app,https://your-domain.com
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ==================== Frontend Configuration ====================
# API Base URL for frontend to connect to backend
# Development: Leave empty (uses Vite proxy to localhost:8000)
# Production: Set to your deployed backend URL
# Example: VITE_API_BASE_URL=https://your-backend.railway.app
VITE_API_BASE_URL=

# ==================== Email Configuration ====================
# For weekly report sending
EMAIL_SENDER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_RECEIVER=recipient@example.com
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# ==================== Model Configuration ====================
# Model Configuration (optional, has defaults)
# Writing model - for text generation, summarization, report writing
WRITING_MODEL_NAME=gpt-4o
WRITING_MODEL_TEMPERATURE=0.2
# Deduce model - for reasoning, judgment, math understanding
DEDUCE_MODEL_NAME=o3-mini
# DEDUCE_MODEL_TEMPERATURE=0
EMBEDDING_MODEL_NAME=text-embedding-3-small

# ==================== Document Processing ====================
# Document Processing (optional, has defaults)
CHUNK_SIZE=1024
CHUNK_OVERLAP=200
